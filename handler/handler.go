package handler

import (
	"context"

	runtime "github.com/shahzaibaziz/graphql"
)

// Handler contains all graphql api handlers
func Handler(ctx context.Context, rt *runtime.Runtime) error {
	return nil
}
